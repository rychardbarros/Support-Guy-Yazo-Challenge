import styled from 'styled-components';
import { Image as ImageC, Flex as FlexC } from '@chakra-ui/react';

export const Image = styled(ImageC)`
  border-radius: 16px;
`;

export const Flex = styled(FlexC)`
  background-color: blue;
  border-radius: 16px;
`;
